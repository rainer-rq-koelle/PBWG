% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apdf_tma.R
\name{create_pbwg_tma_annual_file}
\alias{create_pbwg_tma_annual_file}
\title{Create Canonical Annual PBWG Daily Terminal-Airspace Files from an APDF
Archive}
\usage{
create_pbwg_tma_annual_file(
  zipped_archive_path,
  year,
  reference_data,
  output_dir,
  airports = NULL,
  files = NULL,
  type = c("parquet", "csv", "csv_auto"),
  ranges = c(40, 100),
  region = "EUR",
  max_tma = 180,
  valid_reference_only = TRUE,
  valid_only = TRUE,
  save_augmented = FALSE,
  augmented_dir = output_dir
)
}
\arguments{
\item{zipped_archive_path}{Full path to the APDF ZIP archive.}

\item{year}{Reporting year for the canonical output files.}

\item{reference_data}{A reference tibble from \code{build_tma_reference()}.}

\item{output_dir}{Directory where the daily output files will be written.}

\item{airports}{Optional character vector of ICAO airport codes to keep. If
\code{NULL}, all airports in the prepared summary are written.}

\item{files}{Optional archived files to process.}

\item{type}{File type inside the archive. One of \code{"parquet"}, \code{"csv"}, or
\code{"csv_auto"}.}

\item{ranges}{Numeric vector of ranges in nautical miles to keep.}

\item{region}{Region label included in the output file names.}

\item{max_tma}{Maximum travel time in minutes kept as a candidate sample.}

\item{valid_reference_only}{When \code{TRUE}, only references flagged with
\code{IS_VALID_SAMPLE} are used.}

\item{valid_only}{When \code{TRUE}, only movements with valid terminal-airspace
times are counted in the daily summary.}

\item{save_augmented}{When \code{TRUE}, write the augmented movement-level data
per airport.}

\item{augmented_dir}{Directory where augmented movement-level files will be
written. Defaults to \code{output_dir}.}
}
\value{
A named list with \code{daily_paths} and \code{augmented_paths}.
}
\description{
Processes a yearly APDF archive, applies a chosen reference dataset, and
writes canonical annual PBWG daily terminal-airspace files, one per airport,
to the requested output directory. Optionally, the movement-level augmented
data can be written as well for verification purposes.
}
